apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: avatares-ingress
spec:
  rules:
  - host: avatares8082.example
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx
            port:
              number: 80
